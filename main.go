// Package main implements a simple web application that uses the groupie-tracker package to handle routes and requests.
package main

import (
	. "github.com/erenerdogmus/routes"
)

// main is the entry point of the application.s
func main() {
	HandleRequests()
}
